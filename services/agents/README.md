# Agents

LangGraph orchestration and typed campaign state are implemented in
`apps/api/aevra_api/campaigns`. Graph steps persist safe checkpoints, citations, duration,
and provider metadata. Agents recommend content; they never receive publishing credentials
or unrestricted infrastructure access.
