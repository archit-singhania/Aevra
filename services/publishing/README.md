# Publishing

Deterministic social publisher contracts and platform adapters. This is the only service allowed to decrypt OAuth credentials, and it never exposes them to prompts, browser code, or agent state.

